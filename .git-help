git init                            |    Инициализировать репозиторий
git add *.c                         |    Указать индексируемые файлы по маске
git commit -m 'comment'             |    Зафиксировать изменения в в отслеживаемых файлах с коментарием	
git commit                          |    Зафиксировать изменения и редактировать коментарий
git commit -a -m 'comment'          |    Автоматически комитить все отслеживаемые файлы
git clone git://url/test.git        |    Клонировать репозиторий в новый каталог test
git clone git://url/test.git mytest |    Клонировать репозиторий в каталог mytest
git status                          |    Посмотреть в каком состоянии находятся файлы
git diff                            |    Посмотреть изменения которые не проиндексированны
                                    |    git add file -> vim file -> git diff 
				    |    покажет что изменилось
git diff --cached		    |    Посмотреть что войдет в коммит
git rm file                         |    Удалить файл из индекса и из рабочей папки, сделать коммит
git rm --cached file                |    Удалить файл только из индекса, оставить его в рабочей папке
git mv file_old file_new            |    Переименовать файл
git log				    |    История коммитов
git log -p                          |    История коммитов с указанием изменений (diff)
git log -2                          |    Последние 2 коммита
git log --stat                      |    История коммитов + статистика
git log --graph                     |    История коммитов с ветвлениями и слияниями
git commit --amend                  |    Изменить последний коммит
git reset HEAD file                 |    Отменить индексацию файла
git checkout -- file                |    Отмена изменений файла смомента последнего коммита
git push                            |    Отправить код на репозиторий
git pull                            |    Стянуть свежий код с репозитория 




http://progit.org/book/ru/ch2-5.html
